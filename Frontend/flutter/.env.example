# Public frontend configuration only. Never add OTPs, Firebase service-account
# credentials, database URLs, or SMS provider secrets here.
API_BASE_URL=https://facepay-rtyr.onrender.com

# To use a locally running backend while developing, replace the value above:
# Chrome / desktop: http://localhost:8080
# Android emulator: http://10.0.2.2:8080
# USB Android phone: http://127.0.0.1:8080 (after `adb reverse tcp:8080 tcp:8080`)
# Run: flutter run --dart-define-from-file=.env
